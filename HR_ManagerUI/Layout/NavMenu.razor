<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">HR_Manager</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                🏠 Главная
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="departments">
                🏢 Отделы
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="positions">
                📌 Должности
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="employees">
                👤 Сотрудники
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="persons">
                🧑 Persons
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cities">
                🌆 Cities
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="addresses">
                🏠 Addresses
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="salarygrades">
                💰 Salary Grades
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="employeesalaries">
                💵 Salaries
            </NavLink>
        </div>

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
